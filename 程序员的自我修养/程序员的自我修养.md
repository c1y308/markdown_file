# 目标文件是什么样的
为什么要把程序的指令和数据分开存放？

数据区域对于进程来说是可读可写的，而指令区域对于进程来说只是可读的，可以防止程序的指令被有意或者无意被更改。

另一方面可以提高缓存命中率。

最重要的是当系统运行多个该程序的副本时，指令都是一样的，所以内存中只需要保存一份该程序的指令部分即可。

---

全局变量在汇编阶段也可以通过变量名来获取这个变量所在的地址。**（字符串表的作用）**

